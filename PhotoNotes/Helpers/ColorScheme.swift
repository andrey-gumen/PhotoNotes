import SwiftUI

struct ColorScheme {

    static let viewBackground = Color("viewBackground")
    static let viewForeground = Color("viewForeground")
    
    static let cellBackground = Color("cellBackground")
    

}
